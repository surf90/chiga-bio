# AGENTS.md — chiga-bio 作業ガイド

茅ヶ崎の海辺〜街・里の生き物がわかる、非公式・生物ガイドWebアプリ。
本ファイルは作業時の最重要規約と詳細ドキュメントの所在を集約する。詳細は各ドキュメントへ委譲する（必要時のみ読む）。

---

## プロジェクトの二大原則（最優先）

1. **信頼できるソースからの情報を掲載する**
   - 論文・公的機関（環境省・厚生労働省・日本救急医学会・WoRMS・FishBase・国立科学博物館など）をリライトして掲載する。
   - `references` に最低1件（極力2件）の出典を明記。URLがあれば必ず含める。
   - **原文の転載は禁止**。事実・データを抽出し、必ず自分の言葉で言い換える。

2. **茅ヶ崎に特化した生き物情報を収集・掲載する**
   - `localEncounter` に相模湾・市内の具体地名と季節変動を記述する。
   - **地名はヘッドランドビーチを優先し、サザンビーチはそれより後に記載する**。相模川河口・中央公園なども活用。
   - 市のシンボル生物（市の鳥シジュウカラ・市の花ツツジ・市の木ニセアカシアなど）は制定経緯と市公式HPを併記する。

---

## 技術構成（要点）

- シングルページ静的サイト（Vanilla JS / CSS、外部ライブラリ最小、アイコンはインラインSVG）。
- GitHub Pages 配信・GitHub Actions 自動デプロイ。PWA（`sw.js`）／CSP メタタグ／JSON-LD 構造化データ。
- 主要ファイル: `index.html`・`js/script.js`・`css/style.css`・`data/bio-data.json`・`sw.js`。
- ディレクトリ構成・UX定義・SEO/PWA・Search Console の詳細は **`README.md`** を参照。

---

## データ編集の規約

スキーマ・カテゴリ10分類・環境5区分・`dangerType` ENUM・出力テンプレートは **`adding-biology-data.md` を参照**（特定種の編集時のみ読む）。

最重要ルール（抜粋）:
- 画像 `image` は**プレースホルダーのまま**にする。**URL・撮影者のAI推測は禁止**（人力選定、CC/CC0等の適正ライセンスのみ）。
- 句点ルール: 箇条書き（`features`/`firstAid`）は句点なし、非箇条書き（`localEncounter`/`dontDo`）は句点あり。
- 表記ルール（`README.md` 抜粋）: 難読漢字は各カード初出にふりがな、訓読み和語はひらがな、`など`／`トゲ`／`一年`／`ヒレ`→`ひれ` 等に統一。
- `firstAid`/`dontDo` は医療・公的ガイドライン準拠（**民間療法・個人ブログは禁止**）。

---

## データ変更時の必須手順

1. JSON 妥当性を確認（パースエラーがないこと）。
2. `sw.js` の `CACHE_VERSION` を bump（HTML/CSS/JS/JSON を変更したら必須。配信反映のため）。
3. `progress.md` の「変更履歴」に当日（JST）エントリを追記。
4. `id`・`name`・`scientificName` の一意性、`references` のURL形式を確認。

---

## 言語・命名規約

- 応答・コメント・docstring は日本語。
- ファイル名・`id`・変数名はローマ字／英語（**日本語ファイル名は禁止**）。
- `scientificName`・`license` はラテン字。

---

## トークン効率方針

- Search before Read（Glob/Grep で範囲を絞ってから読む）。
- 特定種の編集時のみ `adding-biology-data.md` を読む。全体俯瞰時は `README.md`／`progress.md`。

---

## Git 運用（最小限）

- `main` が既定ブランチ。変更は作業ブランチを切って PR 経由でマージ（`progress.md` に PR リンクを残す慣例）。
- コミット・push は**ユーザー指示があったときのみ**実行。`main` への直接コミットは避ける。
- コミットメッセージ・PR は日本語。`fix:`／`feat:` 等の接頭辞を踏襲する。
- フック無効化（`--no-verify`）や署名バイパスは行わない。
